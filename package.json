{
  "name": "apirest",
  "version": "1.0.0",
  "description": "API developed with node and express",
  "main": "index.js",
  "scripts": {
    "dev": "SET NODE_ENV=dev && node --env-file=prod.env --watch app.js",
    "start": "SET NODE_ENV=prod && node --env-file=prod.env --watch app.js"
  },
  "author": "jmferreirab",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.2",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=20.10.0",
    "pnpm": ">=8.15.4"
  }
}
